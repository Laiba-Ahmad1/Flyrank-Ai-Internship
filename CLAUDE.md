# Flyrank Ai internship

## Project

Flyrank Ai internship day 2

## Tech Stack

- Next.js
- Javascript
- Git

## Code Style

- Use javascript whenever possible.
- Keep functions small.
- Write descriptive variable names.
- Prefer reusable components.
- Document important logic.

## Git

Use Conventional Commits.

Examples are:

feat:
fix:
docs:
refactor:

## Assignment 2 — Settings Form Rules
- A vague prompt lets the AI invent scope (extra fields, unrequested
  features) instead of building the actual requirement — always specify
  the exact field list, not just "a settings form."
- Visual polish is not evidence of correctness — round 1 looked more
  finished but had no verifiable validation logic; always test the
  actual behavior, not just the appearance.
- [fill in the third rule once you confirm the aria-describedby /
  test result — e.g. "always explicitly ask the AI to run tests before
  declaring done, don't assume 'wrote tests' means 'passing tests'"]